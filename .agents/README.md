# .agents Documentation Hub

This directory contains lightweight, human-readable context for agent sessions.

## Structure

- `memory/` durable project facts and conventions.
- `sessions/` daily session logs and handoff notes.

## Recommended Flow

1. Start with `AGENTS.md`.
2. Review `memory/` for current architecture and constraints.
3. Log important outcomes in the daily session file.
